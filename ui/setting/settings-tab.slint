import { VerticalBox, CheckBox } from "std-widgets.slint";

export component SettingsTab inherits VerticalBox {
    spacing: 20px;
    padding: 16px;
    Text { text: "⚙️ App Settings"; }
    CheckBox { text: "Enable dark mode"; checked: true; }
    CheckBox { text: "Auto-lock vault after 5 minutes"; }
}
